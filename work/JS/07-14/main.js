'use strict'

//함수 정의 형식 : 복잡하거나 반복되는 작업을 하나의 명령어로 간단히 실행

// function fromtoTot() {
//     let tot = 0;
//     let n1 = 1, n2 = 10; //입력

//     for (let n1 = 1; i <= n2; i++) {  // 가공(처리)
//         tot += i;
//     }
//     console.log(tot); // 출력
// }

// fromtoTot(1, 10);

// function fromtoTot(n1, n2) {
//     let tot = 0;

//     for (let n1 = 1; i <= n2; i++) {  // 가공(처리)
//         tot += i;
//     }
//     console.log(tot); // 출력
// }
// // n1, n2가 같은 변수명일 뿐 전역변수와 지역변수로 다른 변수이다.
// let n1 = 1, n2 = 10; //입력
// //입력 부분이 빠져있기 때문에 출력에 값을 넣어줘야 한다.
// fromtoTot(n1, n2);  //실인수

// function fromtoTot(n1, n2) {
//     let tot = 0;

//     for (let i = n1; i <= n2; i++) {  // 가공(처리)
//         tot += i;
//     }
//     return tot; //함수 내 계산 후 return으로 값을 반환해주는 것으로
//                 //계산 값을 출력한다.
// }

// let n1 = 1, n2 = 10; //입력

// console.log(fromtoTot(n1, n2));  //출력
